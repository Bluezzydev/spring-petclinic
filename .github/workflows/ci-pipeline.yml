name: start github actions learing
on: 
  push:
    branches:
      - main
      - dev
      - sit
      - uat
jobs:
  example:
    runs-on: ubuntu-latest
    steps:
      - name: hello world
        run: echo "Hello world"